import React from 'react';
import styled from 'styled-components';

const StyledHome = styled.div`
    position: relative;
    width: 100vw;
    height: 100vh;
`;

export default function Blog() {
    return (<StyledHome>Blog</StyledHome>);
};
